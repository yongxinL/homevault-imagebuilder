[defaults]
# Surface deprecation warnings from modules and keywords. Keeping these
# visible ensures the playbooks get updated before a deprecated feature is
# removed in a future ansible-core release.
deprecation_warnings = False
