# =============================================================================
#
# - Copyright (C) 2016     George Li <yongxinl@outlook.com>
#
# - This is part of HomeVault imagebuilder project.
# - This program is distributed in the hope that it will be useful,
#   but WITHOUT ANY WARRANTY; without even the implied warranty of
#   MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.
#
# =============================================================================

---
- name: update local and create target directory
  file:
    path: "{{ build_image_root }}/casper"
    state: directory

- name: update local and unmount rootfs in build directory
  mount: 
    name: "{{ build_root }}/{{ item }}"
    src: none
    fstype: none
    state: unmounted
  with_items:
    - proc
    - dev
    - dev/pts

- name: update target and remove dpkg policy from rootfs
  file:
    path: "{{ build_root }}/usr/sbin/policy-rc.d"
    state: absent

- name: update target and remove repository list from rootfs
  file:
    path: "{{ build_root }}/{{ item.dest }}"
    state: absent
  with_flattened:
    - "{{ ubuntu_repo_lists }}"

- name: update target and update repository list to rootfs
  lineinfile:
    dest: "{{ build_root }}/{{ item.dest }}"
    line: "{{ item.line }}"
    state: present
    create: yes
  with_items:
    - { dest: '/etc/apt/sources.list', line: 'deb http://au.archive.ubuntu.com/ubuntu/ {{ debstrap_suite }} main' }
    - { dest: '/etc/apt/sources.list.d/universe.list', line: 'deb http://au.archive.ubuntu.com/ubuntu/ {{ debstrap_suite }} universe' }

- name: update local and remove squashfs image if it exists
  file: 
    path: "{{ build_image_root }}/casper/{{ build_base_image }}"
    state: absent

- name: create squashfs image from chroot
  command: >
    mksquashfs {{ build_root }} {{ build_image_root }}/casper/{{ build_base_image }} -e boot -noappend
